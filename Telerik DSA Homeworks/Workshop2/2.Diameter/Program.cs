namespace _2.Diameter
{
    using System;
    using System.Collections.Generic;
    using System.Linq;

    class Program
    {
        static void Main()
        {
            int n = int.Parse(Console.ReadLine());

            var list = new List<int>();
            for (int i = 0; i < n - 1; i++)
            {
                var input = Console.ReadLine().Split(' ').Select(int.Parse);
            }

            Console.WriteLine();
        }
    }
}
